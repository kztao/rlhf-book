<!--
  Copyright (c) 2025-2026 Nathan Lambert.
  Licensed under CC BY-NC-SA 4.0:
  https://creativecommons.org/licenses/by-nc-sa/4.0/
  Full license: https://github.com/natolambert/rlhf-book/blob/main/LICENSE-CHAPTERS
-->
---
prev-chapter: "训练概述"
prev-url: "03-training-overview"
page-title: 指令微调
search-title: "第 4 章：指令微调"
meta-description: "指令微调如何将基础语言模型转变为可用的助手，并为后续的 RLHF 和后训练阶段做好准备。"
next-chapter: "奖励建模"
next-url: "05-reward-models"
lectures:
  - video: "https://www.youtube.com/watch?v=4gIwiSPmQkU&list=PLL1tdVxB1CpVpEtMHxwuR4uI4Lxjw00_y&index=3"
    label: "第 2 讲：IFT、奖励建模、拒绝采样（第 4、5 和 9 章）"
---

# 指令微调

早期的大型预训练语言模型使用下一个 token 预测目标进行训练，默认情况下不带有遵循指令的显式接口。
大约在 GPT-3 发布时 [@brown2020language]，提示和上下文学习成为将单个模型适应多个任务的广泛使用方式（尽管特定任务的微调仍然常见），通过在上下文中展示示例并要求模型完成类似任务。
一个实际的下一步是指令微调，它教导模型以指令-回答的格式作出回应，而不是仅仅继续文本。
例如，给定提示"法国的首都是什么？"，基础模型可能会继续"德国的首都是什么？意大利的首都是什么？……"——简单地扩展问题的模式——而经过指令微调的模型则会回答"法国的首都是巴黎。"

指令微调在两条工作线汇聚时起飞。
首先，NLP 从定制化的微调任务设置转向统一的"文本到文本"或指令框架，这使得标准化多样化数据集和跨多个任务训练单一模型变得简单。
统一任务框架的突出例子包括 *Exploring the Limits of Transfer Learning with a Unified Text-to-Text Transformer*（T5 模型）[@raffel2020exploring]、*Finetuned Language Models Are Zero-Shot Learners*（FLAN 数据集）[@wei2021finetuned]、*Multitask Prompted Training Enables Zero-Shot Task Generalization*（T0 模型）[@sanh2021multitask] 以及 *Cross-Task Generalization via Natural Language Crowdsourcing Instructions*（Natural Instructions 数据集）[@mishra2021cross]。
其次，扩展预训练 LM 以及提示/上下文学习的兴起表明，单一模型可以跨任务泛化，但当模型在指令-回答示例上被显式训练时，这种泛化会变得远更可靠。
这些趋势共同引领了一个在大量指令集合上微调预训练语言模型的时代——现在通常称为指令微调（IFT）或监督微调（SFT），在这种方式下，训练通用模型变得更广泛的受众可及。

自其发现以来，指令微调（也通俗地称为*指令调优*）已经成熟，并成为许多语言建模流程中的标准实践。
就其核心而言，IFT 是将语言模型适应到期望任务分布的最简单方法。
它通过为模型准备一种称为问答的指令格式，作为 RLHF 的基础，并且是那些试图将现代技术应用于新领域的人使用的第一个工具。
没有基本的指令遵循能力水平，我们在本书中讨论的大多数流程——从偏好数据收集到在线 RLHF 优化——都无法进行。

指令微调通常在其他地方被广泛讨论，其核心是监督学习，因此本章侧重于对 RLHF 实践者最重要的实际细节：训练数据如何被格式化和结构化。
关于数据和格式化的决策在后续的训练阶段被直接利用，为模型吸收后训练数据创建了共同语言。

## 聊天模板与指令的结构

后训练过程从定义一个模式开始，该模式用于格式化用户查询，使其能被通过分词器处理信息的语言模型轻松读取。
当使用预训练语言模型时，提示相当简单。模型只知道几个 token：序列开始 token（例如 `<bos_token>`）、序列结束 token（例如 `<eos_token>`）和填充 token（用于管理包含空组件的批次训练）。
这意味着，要提示基础模型，用户输入一个 token 序列供模型继续，例如：

```text
<bos_token> 美国的首都是
```

然后，模型会生成 token，直到其上下文窗口用完，或生成序列结束 token。

所有后训练阶段，从指令微调到 RLHF 和其他方法，都依赖这种格式化来训练模型。
处理与用户交互结构的工具称为**聊天模板**。

下面是一个我们将拆解的示例：

```jinja
{% if messages[0]['role'] == 'system' %}
    {# 如果对话以系统消息开始，将其视为特殊的第一个回合。
       我们设置一个偏移量，使用户/助手交替检查正确对齐。 #}
    {% set offset = 1 %}
{% else %}
    {# 没有系统消息：用户应该是第一个非空回合。 #}
    {% set offset = 0 %}
{% endif %}

{# 发出序列开始 token（模型特定）。 #}
{{ bos_token }}

{# 将每条消息序列化为模型的聊天标记 token。 #}
{% for message in messages %}
    {# 强制角色交替：（system），user，assistant，user，assistant，...
       布尔表达式比较"这是一条用户消息吗？"与当前索引（加偏移量）
       是否预期为用户或助手。 #}
    {% if (message['role'] == 'user') != (loop.index0 % 2 == offset) %}
        {{ raise_exception('对话角色必须交替使用 user/assistant/user/assistant/...') }}
    {% endif %}

    {# 用特殊 token 包装每条消息：
       - <|im_start|><role>\n
       - 消息内容（已修剪）
       - <|im_end|>\n
       这产生一个单一的扁平 token 序列，LM 可以从中预测。 #}
    {{ '<|im_start|>' + message['role'] + '\n' + message['content'] | trim + '<|im_end|>\n' }}
{% endfor %}

{# 可选择附加一个没有内容的"助手"起始标签。
   这提示从助手角色继续生成。 #}
{% if add_generation_prompt %}
    {{ '<|im_start|>assistant\n' }}
{% endif %}
```

这是将包含消息和角色的 Python 字典列表转换为语言模型可以从中预测的 token 的原始代码。

所有传递给模型的信息都被分配了一个角色。
传统的三个角色是 `system`、`user` 和 `assistant`。

`system` 标签仅用于对话的第一条消息；它包含对智能体的指令，这些指令不会从用户那里接收，也不会暴露给用户。
这些**系统提示**用于为模型提供额外的上下文，例如日期和时间，或修补行为。
作为一个有趣的例子，可以告诉模型诸如"你是一个总是以海盗风格回答的友好聊天机器人。"

接下来，另外两个角色简单明了：**user** 持有使用 AI 的人的消息，**assistant** 持有模型的回复（即作为 AI 助手参与）。

为了将所有这些信息转换为 token，我们使用上面开始的代码清单。
模型具有一系列*特殊 token*，用于分隔各种消息。
如果我们用示例查询"人类一次可以吃多少架直升机？"运行上述代码，传递到模型的 token 序列将如下所示：

```text
<|im_start|>system
你是一个总是以海盗风格回答的友好聊天机器人<|im_end|>
<|im_start|>user
人类一次可以吃多少架直升机？<|im_end|>
<|im_start|>assistant
```

注意序列中的最后几个 token 是 `<|im_start|>assistant`。这就是模型知道要继续生成 token 直到最终生成其序列结束 token 的方式，在本例中为 `<|im_end|>`。

通过将所有问答对数据（以及下游偏好微调数据）打包成这种格式，现代语言模型以完美的一致性遵循它。这就是经过指令微调的模型与用户以及在 GPU 或其他计算设备上运行的模型交换信息所使用的语言。

此行为可以朴素地扩展到多轮对话，如下所示：

```text
<|im_start|>system
你是一个总是以海盗风格回答的友好聊天机器人<|im_end|>
<|im_start|>user
人类一次可以吃多少架直升机？<|im_end|>
<|im_start|>assistant
哦，就 6 架。<|im_end|>
<|im_start|>user
你确定吗？<|im_end|>
<|im_start|>assistant
```

在开源生态系统中，将聊天模板应用于消息列表的标准方法是使用存储在分词器配置中的 Jinja 片段，如 `apply_chat_template`。

上述聊天模板是 OpenAI 的聊天标记语言（ChatML）的衍生物，ChatML 是标准化消息格式的早期尝试。
现在，OpenAI 和其他模型提供商使用分层系统，用户可以在其中配置系统消息，但存在可能向用户揭示也可能不揭示的更高级别指令 [@wallace2024instruction]。

还有许多其他聊天模板。一些其他示例包括 Zephyr 的 [@tunstall2023zephyr]：

```text
<|system|>
你是一个总是以海盗风格回答的友好聊天机器人</s>
<|user|>
人类一次可以吃多少架直升机？</s>
<|assistant|>
```

或 Tülu 的：

```text
<|user|>
你怎么样？
<|assistant|>
我只是一个计算机程序，所以没有感情，但我运行正常。今天我能帮你什么？<|endoftext|>
```

除此之外，许多聊天模板包含用于工具使用等任务的格式和其他 token。

## 指令微调的最佳实践

指令微调作为后训练和创建有帮助的语言模型的基础已经很好地确立了。
有许多方法可以实现成功的指令微调。
例如，对某些模型参数量化的高效微调使训练非常易得 [@dettmers2023qlora]。
此外，在狭窄领域如聊天对齐中，即没有更难技能如数学或代码时，小型、专注的数据集可以达到很强的性能 [@zhou2023lima]。

在 ChatGPT 发布后不久，像 No Robots 这样只有 1 万个样本的人类数据集是最先进的 [@no_robots]。
几年后，大规模合成数据集在大多数任务上效果最好 [@lambert2024t]。

一些原则仍然成立：

- 高质量的数据是性能的关键。补全是模型实际学习的内容（在许多情况下，提示不被预测，因此模型不学习预测提示）。
- 大约 100 万个提示可以用来创建能够进行优秀 RLHF 和后训练的模型。进一步扩展仍然有帮助，但回报迅速递减。
- 最好的提示是那些与感兴趣的下游任务分布相似的提示。
- 如果在指令微调之后进行多个阶段的训练，模型可以从指令微调数据中的一些噪声中恢复。精心设计整体优化比执着于每个单独阶段更重要。

## 实现细节

虽然损失函数与预训练中使用的相同，但有一些关键的实现细节与预训练设置不同。
许多实践，如决定用于跨许多 GPU 分片模型的并行类型，与预训练相同，但使用的总机器数量通常较少（对于下面列出的第一个技术变化）：

- **较小的批次大小**：与预训练相比，指令微调（以及其他后训练技术如偏好微调）使用显著更小的批次大小，以在更窄的数据分布上很好地优化，同时保持模型从预训练中的泛化能力。例如，OLMo 2 对 7B 使用 1024 个打包行的批次大小，对 13B 预训练使用 2048，其中这些模型的总上下文长度为 4096 个 token，批次中的每一行是填充序列长度的文档组合。对于后训练，这两个模型仅使用 256 个*提示*的批次大小 [@olmo20242]，不填充到完整序列长度（每批次的非掩码 token 少得多）。较小的批次大小意味着这些训练作业不能像预训练期间那样分片到那么多设备上——在实践中，分布式训练设置具有每个设备的最小批次大小，因此如果你试图为 SFT 保持较小的全局批次大小，你可以累积使用更少的 GPU。在实践中，批次大小迫使每个训练作业的并发 GPU 分配较少并不是限制因素，因为 SFT 的训练 token 计数比预训练中小得多，而且在后训练中需要多个种子的训练以获得最佳最终性能。
- **提示掩码**：在预训练时，批次中的每个 token 都被自回归地预测，然后损失被应用到它们上。对于指令微调，提示 token 被掩码掉，这样模型就不会学习准确预测用户查询——只学习回复。这同样适用于其他后训练算法。
- **多轮掩码**：对于多轮对话，有两种常见的掩码选择。(1) *仅最后一轮*：只有最后一个助手轮的 token 包含在损失中，而所有更早的上下文（包括更早的助手轮）被掩码掉。长对话仍然可以被"展开"成多个训练样本：对于一个 $N$ 轮的对话，每个示例预测一个助手回答，同时掩码掉所有先前的上下文并排除任何未来的轮次。(2) *仅掩码用户轮*：所有用户轮被掩码，但*每个*助手轮都包含在损失中。如果你想获得更多（更短的）训练样本，你仍然可以在此设置中展开，但关键区别是中间的助手回复被直接训练。
- **与预训练相同的损失函数**：指令微调使用与预训练语言模型相同的自回归损失函数，但数据和掩码方式显著不同（仅在整个序列上训练，而预训练文档可以跨批次分割）等。
- **学习率**：SFT 通常使用比预训练小一到两个数量级的学习率，以最好地管理不同的优化动力学（较小的数据集、较小的批次和强大的预训练初始化都支持更保守的更新）。例如，OLMo 2 对预训练使用 $3 \times 10^{-4}$ 的峰值学习率，但对 SFT 使用 $1 \times 10^{-5}$ [@olmo20242]。OLMo 3 使用更高的 SFT 学习率 $5\text{-}8 \times 10^{-5}$ [@teamolmo2025olmo3]，部分原因是其训练基础设施使用序列打包，将多个示例填入每个训练序列并增加了以有用 token 衡量的有效批次大小。更大的批次产生更低方差的梯度估计，这反过来支持更高的学习率而不会不稳定训练——这种关系被称为线性缩放规则。学习率通常在训练步骤的一小部分上预热，然后线性衰减。在实践中，团队通常扫描多个学习率并在保留的评估套件上选择最佳检查点 [@teamolmo2025olmo3]。

## 建议的实验

配套代码仓库在 `code/instruction_tuning/` 中包含一个小型 SFT 训练脚本。
它旨在作为一个学习练习，使基础模型到助手的转变具体化。

1. **运行经典的 SFT 示例并观察基础→助手的转变。**
   运行：

   ```bash
   cd code/
   uv run python -m instruction_tuning.train --config instruction_tuning/configs/sft_olmo2_1b.yaml
   ```

   这将在 `HuggingFaceH4/no_robots` 上训练 `allenai/OLMo-2-0425-1B`（基础模型），并每 50 个优化器步骤打印固定提示池的生成结果。
   在第 0 步，基础模型会跑题、重复提示，并发出格式错误的角色标记；经过几百步后，相同的提示会生成以 `<|endoftext|>` 结束的简洁答案。
   这是指令微调的正确性检查——与预训练相同的损失函数，但应用于带有提示 token 掩码的聊天模板。

2. **扫描学习率。**
   复制 `sft_olmo2_1b.yaml` 并尝试 `lr` 值 `1e-6`、`5e-6` 和 `5e-5`，同时保持其他所有内容不变。
   检查在哪个学习率下模型第一次回答问题并干净地停止，以及在哪个学习率下它过拟合并开始产生模板形状的垃圾输出。
   这是上述"比预训练低一到两个数量级"指导的实际版本。
