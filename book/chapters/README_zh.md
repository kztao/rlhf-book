# RLHF Book 章节

此目录包含 RLHF Book 的 markdown 源文件。

**许可证：** [CC-BY-NC-SA-4.0](../../LICENSE-CHAPTERS)

在线阅读本书 [rlhfbook.com](https://rlhfbook.com)。

## SEO 前页

章节的 `page-title` 和 `meta-description` 值在 HTML 构建中被序列化为 JSON-LD。更改 SEO 前页后重新运行生成的 JSON-LD 验证。
